const router = require('express').Router();
/* on require mysql pour l'utiliser */
// const mysql = require('mysql');

// Create connection a la bdd sql
/*
const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'meteo'
});


*/
module.exports = router;

